<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="{{path('user')}}"><img src="{{ asset('images/logo.jpg.webp') }}" style="height: 100px"></a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav m-auto">
            {% if user != null %}
                {% if user.secteur == 'Secrétaire' %}
                    <li class="nav-item ">
                        <a class="nav-link text-primary font-weight-bold" href="{{ path('app_register') }}"> Ajouter un employée </a>
                    </li>
                {% endif %}
            {% endif %}
            {% if user != null %}
                <li class="nav-item ">
                    <a class="nav-link text-danger font-weight-bold" href="{{ path('app_logout') }}">Se déconnecter</a>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>